{
  "name": "marp",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "preview": "marp slide.md --theme theme/slide.css --html --preview --allow-local-files",
    "pptx": "marp slide.md --theme theme/slide.css --html --allow-local-files --pptx",
    "pdf": "marp slide.md --theme theme/slide.css --html --pdf --allow-local-files"
  },
  "devDependencies": {
    "@marp-team/marp-cli": "^4.1.1"
  }
}